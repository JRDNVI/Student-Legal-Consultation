name: Build
on:
  workflow_call:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  build:
    name: Build
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: build
        run: |
            cd frontend/EduCaseFrontend
            npm ci
            npm run build
      - uses: actions/upload-artifact@v4
        with:
          name: build-${{ github.run_number }}
          path: |
            dist
            *firebase            
           